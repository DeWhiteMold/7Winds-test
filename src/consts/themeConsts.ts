import { createTheme, } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: '#FFFFFF'
    },
    secondary: {
      main: '#A1A1AA'
    },
    info: {
      main: '#7890B2'
    }
  },
})

export { theme, }